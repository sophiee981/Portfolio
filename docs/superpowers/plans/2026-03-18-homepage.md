# Homepage Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Rebuild the `/` Home page with Tailwind utilities — Navbar, Hero, Featured Projects, Footer — matching spec.md.

**Architecture:** Replace all custom CSS classes with Tailwind v4 utility classes using the design tokens from `index.css`. Home page shows only Hero + Featured Projects + Footer. Nav links route to separate pages (`/projects`, `/about`, `/contact`).

**Tech Stack:** React 18, TypeScript, Tailwind CSS v4, React Router v6, Lucide React, CVA components in `src/components/ui/`

---

### Task 1: Update content.ts — add `featured` flag and social links

**Files:**
- Modify: `src/data/content.ts`

- [ ] Add `featured?: boolean` to `CaseStudy` type and mark first 3 as `featured: true`
- [ ] Add `twitter` and social object to `profile`
- [ ] Commit: `feat(data): add featured flag and twitter social link`

---

### Task 2: Rebuild Navbar with Tailwind utilities

**Files:**
- Modify: `src/components/Navbar.tsx`

- [ ] Replace all custom CSS class names with Tailwind utilities
- [ ] Nav links: `Home→/`, `Projects→/projects`, `About→/about`, `Contact→/contact` (router `<Link>`)
- [ ] Sticky header: `fixed top-0 left-0 right-0 z-50`
- [ ] Scrolled state: `bg-background/80 backdrop-blur-md border-b border-border`
- [ ] Logo: `font-bold text-lg tracking-tight` with accent dot
- [ ] Mobile hamburger + slide-down menu
- [ ] Run: `npm run build` — expect 0 errors
- [ ] Commit: `feat(navbar): rebuild with Tailwind utilities`

---

### Task 3: Rebuild Hero section with Tailwind utilities

**Files:**
- Modify: `src/components/Hero.tsx`

- [ ] Replace custom CSS classes with Tailwind utilities
- [ ] Full-viewport height: `min-h-svh flex flex-col justify-center pt-16`
- [ ] Available badge: green dot pulse + `border border-green-500/40 bg-green-500/10 text-green-400`
- [ ] Name: `text-6xl font-extrabold tracking-tighter` (clamp via `text-5xl md:text-7xl lg:text-8xl`)
- [ ] Role: accent color `text-[var(--portfolio-accent)]`
- [ ] CTA buttons use `<Button>` component from `src/components/ui/button.tsx`
- [ ] Purple glow blob background decoration
- [ ] Scroll indicator line animation
- [ ] Run: `npm run build` — expect 0 errors
- [ ] Commit: `feat(hero): rebuild with Tailwind utilities`

---

### Task 4: Create FeaturedProjects component

**Files:**
- Create: `src/components/FeaturedProjects.tsx`
- Modify: `src/pages/Home.tsx`

- [ ] New component: filter `caseStudies.filter(cs => cs.featured)` — max 3
- [ ] Section label: `text-xs font-semibold uppercase tracking-widest text-[var(--portfolio-accent)]`
- [ ] Grid: first card full-width (`col-span-2`), next two side-by-side — `grid grid-cols-1 md:grid-cols-2 gap-4`
- [ ] Each card uses `<Card>` from `src/components/ui/card.tsx`
- [ ] Cover placeholder: colored div with project initial letter
- [ ] Tags use `<Badge variant="outline">` from `src/components/ui/badge.tsx`
- [ ] Hover: `group` + `group-hover:scale-[1.02] transition-transform`
- [ ] "View all work" link → `/projects`
- [ ] Update `Home.tsx`: import FeaturedProjects, remove About/Timeline/Contact
- [ ] Run: `npm run build` — expect 0 errors
- [ ] Commit: `feat(featured-projects): new component with Card+Badge UI`

---

### Task 5: Rebuild Footer with Tailwind utilities + social links

**Files:**
- Modify: `src/components/Footer.tsx`

- [ ] Replace custom CSS with Tailwind utilities
- [ ] Social icon links: LinkedIn, Behance, Dribbble, X (Twitter) — from `profile` data
- [ ] Layout: `flex flex-col sm:flex-row items-center justify-between gap-4`
- [ ] Social links: icon + hover `text-muted-foreground hover:text-foreground transition-colors`
- [ ] Use Lucide icons where available; fallback to text initials
- [ ] Copyright left, socials right
- [ ] Run: `npm run build` — expect 0 errors
- [ ] Commit: `feat(footer): add social links with Tailwind utilities`

---

### Task 6: Clean up App.css

**Files:**
- Modify: `src/App.css`

- [ ] Remove all homepage-specific custom CSS blocks (navbar, hero, work-card, footer)
- [ ] Keep only: Case Study page styles (`.case-study*`), Timeline styles (`.timeline*`), About styles (`.about*`) — these aren't rebuilt yet
- [ ] Run: `npm run build` — expect 0 errors
- [ ] Run: `npm run dev` — visually verify homepage looks correct
- [ ] Commit: `chore(css): remove homepage custom CSS replaced by Tailwind`
